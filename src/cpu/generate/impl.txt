cdef int XOR_A_B(GBCPU):
    cpu.registers[REG_A] ^= cpu.registers[REG_B]
    cpu.F = 0
    if cpu.registers[REG_A] == 0:
        cpu.F = FLAG_Z
    return 4

cdef int XOR_A_C(GBCPU):
    cpu.registers[REG_A] ^= cpu.registers[REG_C]
    cpu.F = 0
    if cpu.registers[REG_A] == 0:
        cpu.F = FLAG_Z
    return 4

cdef int XOR_A_D(GBCPU):
    cpu.registers[REG_A] ^= cpu.registers[REG_D]
    cpu.F = 0
    if cpu.registers[REG_A] == 0:
        cpu.F = FLAG_Z
    return 4

cdef int XOR_A_E(GBCPU):
    cpu.registers[REG_A] ^= cpu.registers[REG_E]
    cpu.F = 0
    if cpu.registers[REG_A] == 0:
        cpu.F = FLAG_Z
    return 4

cdef int XOR_A_H(GBCPU):
    cpu.registers[REG_A] ^= cpu.registers[REG_H]
    cpu.F = 0
    if cpu.registers[REG_A] == 0:
        cpu.F = FLAG_Z
    return 4

cdef int XOR_A_L(GBCPU):
    cpu.registers[REG_A] ^= cpu.registers[REG_L]
    cpu.F = 0
    if cpu.registers[REG_A] == 0:
        cpu.F = FLAG_Z
    return 4

cdef int XOR_A_A(GBCPU):
    cpu.registers[REG_A] ^= cpu.registers[REG_A]
    cpu.F = 0
    if cpu.registers[REG_A] == 0:
        cpu.F = FLAG_Z
    return 4

cdef int XOR_A_HL(GBCPU):
    cpu.registers[REG_A] ^= cpu.mem.read8(cpu.get_HL())
    cpu.F = 0
    if cpu.registers[REG_A] == 0:
        cpu.F = FLAG_Z
    return 8

